---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout pageBg="bases" title="Base de Datos - Hoteles | AdmiLogistic" description="Mapa y listado de hoteles para transportistas con parking para camiones.">
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY=" crossorigin="" />
  <div class="db-page">
    <header class="db-header">
      <h1>Hoteles para transportistas</h1>
      <p>Alojamientos con parking para camiones. Hoteles, hostales y moteles en rutas nacionales. Datos orientativos.</p>
    </header>
    <div class="db-map-wrap">
      <div id="map-hoteles" class="db-map"></div>
    </div>
    <div class="db-list-wrap">
      <h2>Listado</h2>
      <div id="list-hoteles" class="db-list"></div>
    </div>
    <div class="db-cta">
      <a href="/proyectos/roadmaster" class="db-cta-btn">Usar hoteles en RoadMaster</a>
    </div>
  </div>
  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js" integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo=" crossorigin="" defer></script>
  <script>
    (function () {
      const mapEl = document.getElementById('map-hoteles');
      const listEl = document.getElementById('list-hoteles');
      if (!mapEl) return;
      fetch('/data/hoteles.json')
        .then(r => r.json())
        .then(data => {
          if (typeof L === 'undefined') return;
          const map = L.map('map-hoteles').setView([40.4, -3.7], 6);
          L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', { attribution: '© OpenStreetMap' }).addTo(map);
          const bounds = [];
          data.forEach(p => {
            const lat = p.lat, lng = p.lng;
            if (lat == null || lng == null) return;
            bounds.push([lat, lng]);
            const name = p.nombre || '';
            const info = [p.direccion || p.provincia, p.precio_desde != null ? 'desde ' + p.precio_desde + ' €' : ''].filter(Boolean).join(' · ');
            L.marker([lat, lng]).addTo(map).bindPopup(`<strong>${escapeHtml(name)}</strong><br>${escapeHtml(info)}`);
          });
          if (bounds.length) map.fitBounds(bounds, { padding: [30, 30], maxZoom: 10 });
          if (listEl && data.length) {
            listEl.innerHTML = data.map(p => {
              const info = [p.direccion || p.provincia, p.precio_desde != null ? 'desde ' + p.precio_desde + ' €' : ''].filter(Boolean).join(' · ');
              const stars = p.estrellas ? '★'.repeat(p.estrellas) + ' ' : '';
              return `<div class="db-list-item"><strong>${stars}${escapeHtml(p.nombre)}</strong><br><span class="db-list-meta">${escapeHtml(info)}</span></div>`;
            }).join('');
          }
        })
        .catch(() => { if (listEl) listEl.innerHTML = '<p>No se pudieron cargar los datos.</p>'; });
      function escapeHtml(s) {
        if (!s) return '';
        const d = document.createElement('div');
        d.textContent = s;
        return d.innerHTML;
      }
    })();
  </script>
</BaseLayout>

<style>
  .db-page { max-width: 1200px; margin: 0 auto; padding: 1.5rem 1rem 3rem; }
  .db-header { text-align: center; margin-bottom: 1.5rem; }
  .db-header h1 { font-size: clamp(1.75rem, 3vw, 2.25rem); color: #1a1a1a; margin-bottom: 0.5rem; }
  .db-header p { color: #555; font-size: 1rem; line-height: 1.6; }
  .db-map-wrap { margin-bottom: 2rem; border-radius: 16px; overflow: hidden; box-shadow: 0 12px 40px rgba(0,0,0,0.12); border: 1px solid rgba(0,0,0,0.08); }
  .db-map { height: 420px; background: #e5e7eb; }
  .db-list-wrap h2 { font-size: 1.25rem; margin-bottom: 1rem; color: #1a1a1a; }
  .db-list { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 1rem; }
  .db-list-item { padding: 1rem; background: rgba(255,255,255,0.9); border-radius: 10px; border-left: 4px solid #8b5cf6; font-size: 0.9375rem; }
  .db-list-item strong { display: block; margin-bottom: 0.25rem; color: #0f172a; }
  .db-list-meta { color: #64748b; }
  .db-cta { text-align: center; margin-top: 2rem; }
  .db-cta-btn { display: inline-block; padding: 0.75rem 1.75rem; background: #8b5cf6; color: #fff; font-weight: 600; text-decoration: none; border-radius: 10px; transition: background 0.2s; }
  .db-cta-btn:hover { background: #7c3aed; }
  @media (max-width: 768px) { .db-map { height: 320px; } .db-list { grid-template-columns: 1fr; } }
</style>
